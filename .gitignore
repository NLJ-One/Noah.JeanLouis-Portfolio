# Ignore heavy video source files (GitHub limit: 100MB per file)
images/**/*.mp4
images/**/*.mov
images/**/*.MP4
images/**/*.MOV

# Keep only lightweight web videos in the repository
!images/Photo_Vidéo/*-web.mp4
